  🚀 PREFLIGHT · convening the crew…

════════════════════════════════════════════════════════════
  🧑‍🚀 MISSION CONTROL — HOLD (32/100, goal 85)
  ██████░░░░░░░░░░░░░░
  🚧 6 blockers  🧹 10 nits
  Critical crash risks, silent boot failures, and build artifacts in git. Zero tests.
────────────────────────────────────────────────────────────
  🔥 ROASTER  Build junk in git, uncaught TypeErrors, leaky logs, weak error handling.
  🦣 MAMMOTH  Artifacts committed, missing tests, swallowed errors, fragile types.

  ⬆  RAISE THE SCORE
     • Fix unhandled TypeErrors in json.loads and widen exception guards in server.py
     • Add logging to the bare except block in http_shim.py to stop silent boot failures
     • Remove build/ directory from source control and add to .gitignore
════════════════════════════════════════════════════════════
  🔥 ROASTER · 8 notes
   HIGH server.py:_run_query
        json.loads on non-string filters/sorts raises TypeError, not caught by _call_tool's (KeyError, ValueError) guard
        “json.loads eats strings, not dicts. TypeError ain't ValueError—boom, unhandled. Fix guard.”
   HIGH http_shim.py:module level
        except Exception swallows startup failures silently with no logging
        “Swallowing all startup exceptions without a log burp? 503 mystery meat. Log the wound.”
   HIGH server.py:_run_query / _call_tool
        json.loads on non-string input raises TypeError, not caught
        “Pass a number for filters? `json.loads` throws TypeError, catch block snoozes. Crash.”
   MED  server.py:build_server
        httpx.AsyncClient leaks if telemetry.setup_telemetry raises after client creation
        “Create client then crash on telemetry? Socket go brrr, never closed. Re-order or try/finally.”
   MED  server.py:_call_tool
        Narrow except (KeyError, ValueError) lets other handler exceptions escape as unhandled errors
        “Only catching KeyError and ValueError? One surprise TypeError and your MCP frame weeps. Widen net.”
   MED  build/lib/modular_bos_mcp/schema.py
        Generated build artifact committed to source control
        “Build folder in git? Stale bread. Ignore it.”
   MED  telemetry.py:track
        json.dumps on tool args crashes if values aren't JSON-serializable
        “One weird datetime and your span dies first.”
   MED  src/modular_bos_mcp/omni_client.py:68-115
        list_topics and validate_model assume resp is list or dict; crash otherwise
        “Omni returns null? AttributeError party. Type-check your JSON, caveman.”
  🦣 MAMMOTH · 8 notes
   HIGH build/lib/modular_bos_mcp/server.py
        Python build artifact committed to source control
        “Commit build/ dir. Generated code in repo bad.”
   HIGH http_shim.py:48
        Bare except Exception at boot swallows startup failures without logging
        “Swallow boot error, no log. Blind when server sick.”
   HIGH CloudInfra/mcp-servers/bos/build/lib/modular_bos_mcp/__init__.py
        Build artifact directory committed to source control.
        “Build lib must not live in repo. Remove and gitignore.”
   MED  server.py and http_shim.py
        No unit tests for handlers, schema builders, or HTTP shim
        “Zero tests. Ship without net.”
   MED  http_shim.py:44-51
        Config parsing and server construction run at module import time
        “Import-time side effects. Hard to test, hard to mock.”
   MED  server.py:411-415
        Tool dispatch only catches KeyError/ValueError; other exceptions escape unhandled
        “Catch too narrow. Unexpected errors leak out.”
   MED  server.py:95-180 area
        Six Omni handlers duplicate identical telemetry/track + try/except boilerplate.
        “DRY handler wrapping—one decorator cuts six copy-paste error blocks.”
   MED  server.py:_meta
        Meta response leaks internal repo path and codename ('infra beaver cluster_mcp_grafana.py').
        “Scrub internal repo paths from API surface; operators can read docs instead.”

